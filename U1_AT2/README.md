1.	Implemented a simple constraint-based scheduling system using Backtracking logic.
Checked all given constraints before assigning doctors to shifts.
Obtained a valid schedule where every doctor is assigned exactly one suitable shift.

2.	Implemented the Breadth-First Search (BFS) algorithm to find the shortest path. 
The robot avoided obstacles and explored nodes level by level. 
The program successfully reached the goal with the minimum path cost.

3.	Implemented a rescue robot using the Uniform Cost Search (UCS) algorithm. 
The robot avoided blocked paths and selected the least-cost route. 
The program successfully reached the survivor while minimizing the total travel cost. 
